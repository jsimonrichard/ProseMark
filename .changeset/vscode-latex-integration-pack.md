---
"vscode-prosemark": patch
"vscode-prosemark-latex-integration": patch
---

Add **vscode-prosemark-latex-integration** (initial `0.0.0` in repo; first release becomes `0.0.1`) to the ProseMark **extension pack**. LaTeX webview defers loading `@prosemark/latex` until the editor exists and merges into `extraCodeMirrorExtensions` with other integrations.
